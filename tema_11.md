# Тема 11. Итераторы и генераторы.
Отчет по Теме #11 выполнил(а):
- Хисматуллин Андрей Дмитриевич
- ПИЭ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + |
| Задание 4 | + |
| Задание 5 | + |


знак '+' - задание выполнено; знак '-' - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Простой итератор, но у него нет гибкой настройки, например его нельзя развернуть. Он работает просто как next(), но нет prev()

```python
numbers = [0, 1, 2, 3, 4, 5]
for item in numbers:
    print(item)
```

### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/l1.png)

## Выводы
Код выводит на экран каждое число из списка numbers, который содержит значения от 0 до 5.

## Лабораторная работа №2
### Класс итератор с гибкой настройкой и удобными применением

```python
class CountDown:
    def __init__(self, start):
        self.count = start + 1
    def __iter__(self):
        return self
    def __next__(self):
        self.count -= 1
        if self.count < 0:
            raise StopIteration
        return self.count

if __name__ == '__main__':
    counter = CountDown(5)
    for i in counter:
        print(i)
```
### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/l2.png)

## Выводы
Код создает класс CountDown, который итеративно выводит числа от 5 до 0 в обратном порядке.

## Лабораторная работа №3
### Генератор списка

```python
a = [i ** 2 for i in range(1, 5)]

print('a - ', a)
for i in a:
    print(i)

print('iter(a) - ', iter(a))
for i in a:
    print(i)
```
### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/l3.png)

## Выводы
Код создает список квадратов чисел от 1 до 4, выводит его на экран и затем повторно итерирует по нему

## Лабораторная работа №4
### Выражения генераторы

```python
b = (i ** 2 for i in range(1, 5))
print(b)
print('first')
for i in b:
    print(i)
print('second')
for i in b:
    print(i)
```
### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/l4.png)

## Выводы
Код проверяет длину строки name и либо выводит «Успешная регистрация», либо вызывает собственное исключение NegativeValueException с сообщением «Длина более 10 символов».

## Лабораторная работа №5
### Такой же счетчик, как и в первом задании, только это генератор и использует yield

```python
def countdown(count):
    while count >= 0:
        yield count
        count -= 1

if __name__ == '__main__':
    counter = countdown(5)
    for i in counter:
        print(i)
```
### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/l5.png)

## Выводы
Код создает генератор квадратов чисел от 1 до 4, выводит его объект, а затем итерирует по нему дважды, при этом во втором проходе не выведет ничего, так как генератор уже исчерпан.

## Самостоятельная работа №1
### Вас никак не могут оставить числа Фибоначчи, очень уж они вас заинтересовали. Изучив новые возможности Python вы решили реализовать программу, которая считает числа Фибоначчи при помощи итераторов. Расчет начинается с чисел 1 и 1. Создайте функцию fib(n), генерирующую n чисел Фибоначчи с минимальными затратами ресурсов. Для реализации этой функции потребуется обратиться к инструкции yield (Она не сохраняет в оперативной памяти огромную последовательность, а дает возможность “доставать” промежуточные результаты по одному). Результатом решения задачи будет листинг кода и вывод в консоль с числом Фибоначчи от 200.

```python
def fib(n):
    a, b = 1, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

n = 200
last = None
for i, val in enumerate(fib(n), start=1):
    last = val
print(last)
```
### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/s1.png)

## Выводы
Код генерирует первые 200 чисел Фибоначчи и выводит последнее из них.

## Самостоятельная работа №2
### К коду предыдущей задачи добавьте запоминание каждого числа Фибоначчи в файл “fib.txt”, при этом каждое число должно находиться на отдельной строчке. Результатом выполнения задачи будет листинг кода и скриншот получившегося файла

```python
def fib(n):
    a, b = 1, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

n = 200
last = None
with open('C:/Users/ivan7/Desktop/лабы/пи/11/fib.txt', 'w', encoding='utf-8') as f:
    for i, v in enumerate(fib(n), start=1):
        f.write(str(v) + "\n")
        last = v
print(last)
```

### Результат.
![Меню](https://github.com/KhismatullinFl/SoftwareEngineering/blob/tema_11/images/s2.png)

## Выводы
Код генерирует первые 200 чисел Фибоначчи, записывает их в файл и выводит из них.

## Общие выводы по теме
В ходе лабораторной работы по теме 'Итераторы и генераторы' я освоил принципы создания и использования итераторов и генераторов в Python. Я научился реализовывать собственные итераторы, используя методы __iter__() и __next__(), а также создавать генераторы с помощью функции yield. Это позволило мне эффективно управлять памятью и работать с большими объемами данных. Это поможет мне при решении дальнейших задач.
